import { serviceContract } from "./modules/services/service.contract";
import { sessionContract } from "./modules/session/session.contract";

export const contract = {
    service: serviceContract,
    session: sessionContract,
};
